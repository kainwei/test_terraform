image: docker:latest
# When using dind, it's wise to use the overlayfs driver for
# improved performance.
variables:
  DOCKER_DRIVER: overlay
  DOCKER_TLS_CERTDIR: ''
services:
- docker:dind
before_script:
- docker info
build:
  script: 
   - docker info
